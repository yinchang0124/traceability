<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.keer.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scanActivity" />
        <activity android:name=".PigInfoActivity" />
        <activity android:name=".ValueActivity" />
        <activity android:name=".buyActivity" />
        <activity android:name=".sellActivity" />
        <activity android:name=".addpigActivity" />
        <activity android:name=".sellScanActivity" />
        <activity android:name=".sellpiginfoActivity" />
        <activity android:name=".buyinfoActivity"></activity>
    </application>

</manifest>